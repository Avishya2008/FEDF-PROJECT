<!DOCTYPE html>
<html lang="en">

<head>

<meta charset="UTF-8">

<meta name="viewport"
content="width=device-width, initial-scale=1.0">

<title>
Anti-Ragging Complaint Portal
</title>

<link
href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap"
rel="stylesheet">

<style>

*{
margin:0;
padding:0;
box-sizing:border-box;
font-family:'Poppins',sans-serif;
}

body{
background:
linear-gradient(
135deg,
#0f172a,
#1e293b,
#2563eb
);
min-height:100vh;
color:white;
}

header{
display:flex;
justify-content:space-between;
align-items:center;
padding:20px 40px;
background:
rgba(255,255,255,0.1);
backdrop-filter:blur(10px);
box-shadow:
0 4px 20px rgba(0,0,0,0.2);
}

.logo{
font-size:28px;
font-weight:700;
}

nav button{
padding:10px 18px;
border:none;
border-radius:8px;
cursor:pointer;
font-weight:600;
margin-left:10px;
}

.container{
width:90%;
max-width:1200px;
margin:auto;
padding:30px 0;
}

.card{
background:white;
color:black;
padding:25px;
border-radius:15px;
margin-bottom:20px;
box-shadow:
0 10px 20px rgba(0,0,0,0.2);
}

input,
textarea,
select{
width:100%;
padding:12px;
margin:10px 0;
border:1px solid #ddd;
border-radius:8px;
font-size:15px;
}

textarea{
resize:none;
}

button{
padding:10px 15px;
border:none;
border-radius:8px;
cursor:pointer;
font-weight:600;
}

.primary{
background:#2563eb;
color:white;
}

.success{
background:#16a34a;
color:white;
}

.warning{
background:#f59e0b;
color:white;
}

.danger{
background:#dc2626;
color:white;
}

.hidden{
display:none;
}

.dashboard{
display:grid;
grid-template-columns:
repeat(auto-fit,minmax(220px,1fr));
gap:20px;
margin-bottom:20px;
}

.stat{
background:white;
color:black;
padding:20px;
border-radius:15px;
text-align:center;
box-shadow:
0 5px 15px rgba(0,0,0,0.2);
}

.stat h2{
font-size:35px;
margin-bottom:10px;
}

table{
width:100%;
border-collapse:collapse;
margin-top:15px;
}

th,
td{
border:1px solid #ddd;
padding:10px;
text-align:left;
}

th{
background:#2563eb;
color:white;
}

.notification{
background:#dbeafe;
color:black;
padding:12px;
margin-bottom:10px;
border-radius:8px;
}

.action-btn{
padding:6px 10px;
font-size:12px;
margin:2px;
}

.section-title{
margin-bottom:15px;
font-size:24px;
font-weight:600;
}

</style>

</head>

<body>

<header>

<div class="logo">
🛡 Anti-Ragging Portal
</div>

<nav>

<button
id="loginBtn"
onclick="showLogin()">
Login
</button>

<button
id="logoutBtn"
onclick="logout()"
style="display:none;">
Logout
</button>

</nav>

</header>

<div class="container">

<!-- LOGIN PAGE -->

<div
id="loginPage"
class="card">

<h2 class="section-title">
Login / Register
</h2>

<input
type="text"
id="username"
placeholder="Username">

<input
type="password"
id="password"
placeholder="Password">

<select id="role">

<option value="student">
Student
</option>

<option value="admin">
Admin
</option>

</select>

<button
class="primary"
onclick="registerUser()">

Register

</button>

<button
class="success"
onclick="loginUser()">

Login

</button>

</div>
<!-- STUDENT DASHBOARD -->

<div
id="studentDashboard"
class="hidden">

<div class="dashboard">

<div class="stat">
<h2 id="studentComplaints">0</h2>
<p>Total Complaints</p>
</div>

<div class="stat">
<h2 id="studentPending">0</h2>
<p>Pending Cases</p>
</div>

<div class="stat">
<h2 id="studentResolved">0</h2>
<p>Resolved Cases</p>
</div>

</div>

<div class="card">

<h2 class="section-title">
Student Services
</h2>

<button
class="primary"
onclick="showPage('complaintForm')">

📝 Submit Complaint

</button>

<button
class="success"
onclick="showPage('trackComplaintSection')">

🔍 Track Complaint

</button>

<button
class="warning"
onclick="showHistory()">

📜 Complaint History

</button>

<button
class="danger"
onclick="showNotifications()">

🔔 Notifications

</button>

</div>

</div>

<!-- ADMIN DASHBOARD -->

<div
id="adminDashboard"
class="hidden">

<div class="dashboard">

<div class="stat">

<h2 id="totalComplaints">
0
</h2>

<p>
Total Complaints
</p>

</div>

<div class="stat">

<h2 id="pendingComplaints">
0
</h2>

<p>
Pending Cases
</p>

</div>

<div class="stat">

<h2 id="resolvedComplaints">
0
</h2>

<p>
Resolved Cases
</p>

</div>

</div>

<div class="card">

<h2 class="section-title">
Admin Controls
</h2>

<button
class="primary"
onclick="loadComplaintsAdmin()">

Manage Complaints

</button>

<button
class="success"
onclick="exportCSV()">

Export Report

</button>

<button
class="warning"
onclick="showHistory()">

Complaint History

</button>

<button
class="primary"
onclick="showPage('trackComplaintSection')">

Status Tracking

</button>

<button
class="danger"
onclick="showCommitteeNotes()">

Committee Notes

</button>

</div>

</div>

<!-- COMPLAINT FORM -->

<div
id="complaintForm"
class="card hidden">

<h2 class="section-title">
Submit Complaint
</h2>

<input
id="complaintTitle"
placeholder="Complaint Title">

<input
id="complaintLocation"
placeholder="Incident Location">

<textarea
id="complaintDescription"
rows="6"
placeholder="Describe the ragging incident in detail">
</textarea>

<label>

<input
type="checkbox"
id="anonymous">

Submit Anonymously

</label>

<br><br>

<button
class="primary"
onclick="submitComplaint()">

Submit Complaint

</button>

</div>

<!-- TRACK COMPLAINT -->

<div
id="trackComplaintSection"
class="card hidden">

<h2 class="section-title">
Track Complaint
</h2>

<input
id="trackId"
placeholder="Enter Complaint ID">

<button
class="success"
onclick="trackComplaint()">

Track Status

</button>

<br><br>

<div id="trackResult"></div>

</div>
<!-- COMPLAINT HISTORY -->

<div
id="historySection"
class="card hidden">

<h2 class="section-title">
Complaint History
</h2>

<div id="historyData"></div>

</div>

<!-- NOTIFICATIONS -->

<div
id="notificationSection"
class="card hidden">

<h2 class="section-title">
Notifications
</h2>

<div id="notificationData"></div>

</div>

<!-- COMMITTEE NOTES -->

<div
id="committeeNotesSection"
class="card hidden">

<h2 class="section-title">
Committee Notes
</h2>

<textarea
id="generalCommitteeNote"
rows="6"
placeholder="Write committee notes here">
</textarea>

<br><br>

<button
class="primary"
onclick="saveGeneralNote()">

Save Note

</button>

<br><br>

<div id="savedNoteArea"></div>

</div>

<!-- ADMIN COMPLAINT MANAGEMENT -->

<div
id="adminComplaints"
class="card hidden">

<h2 class="section-title">
Complaint Management
</h2>

<div id="adminComplaintData"></div>

</div>

</div>

<!-- JAVASCRIPT STARTS HERE -->

<script>

let currentUser = "";
let currentRole = "";

function showLogin(){

document.getElementById(
"loginPage"
).classList.remove("hidden");

}

function showPage(pageId){

const sections = [

"complaintForm",

"trackComplaintSection",

"historySection",

"notificationSection",

"adminComplaints",

"committeeNotesSection"

];

sections.forEach(section=>{

let element =
document.getElementById(section);

if(element){

element.classList.add("hidden");

}

});

document
.getElementById(pageId)
.classList.remove("hidden");

}
function registerUser(){

let username =
document.getElementById(
"username"
).value.trim();

let password =
document.getElementById(
"password"
).value.trim();

let role =
document.getElementById(
"role"
).value;

if(
username === "" ||
password === ""
){

alert(
"Please fill all fields"
);

return;

}

let users =
JSON.parse(
localStorage.getItem(
"users"
)
) || [];

let exists =
users.find(
u =>
u.username === username
);

if(exists){

alert(
"Username already exists"
);

return;

}

users.push({

username:username,

password:password,

role:role

});

localStorage.setItem(

"users",

JSON.stringify(users)

);

alert(
"Registration Successful"
);

}

function loginUser(){

let username =
document.getElementById(
"username"
).value.trim();

let password =
document.getElementById(
"password"
).value.trim();

let role =
document.getElementById(
"role"
).value;

let users =
JSON.parse(
localStorage.getItem(
"users"
)
) || [];

let user =
users.find(

u =>

u.username === username &&

u.password === password &&

u.role === role

);

if(!user){

alert(
"Invalid Credentials"
);

return;

}

currentUser = username;
currentRole = role;

document.getElementById(
"loginPage"
).classList.add("hidden");

document.getElementById(
"loginBtn"
).style.display =
"none";

document.getElementById(
"logoutBtn"
).style.display =
"inline-block";

if(role === "student"){

document.getElementById(
"studentDashboard"
).classList.remove(
"hidden"
);

loadStudentStats();

}

if(role === "admin"){

document.getElementById(
"adminDashboard"
).classList.remove(
"hidden"
);

loadAdminStats();

}

alert(
"Login Successful"
);

}

function logout(){

currentUser = "";
currentRole = "";

document.getElementById(
"studentDashboard"
).classList.add(
"hidden"
);

document.getElementById(
"adminDashboard"
).classList.add(
"hidden"
);

document.getElementById(
"loginPage"
).classList.remove(
"hidden"
);

document.getElementById(
"loginBtn"
).style.display =
"inline-block";

document.getElementById(
"logoutBtn"
).style.display =
"none";

const sections = [

"complaintForm",
"trackComplaintSection",
"historySection",
"notificationSection",
"adminComplaints",
"committeeNotesSection"

];

sections.forEach(section=>{

let el =
document.getElementById(
section
);

if(el){

el.classList.add(
"hidden"
);

}

});

}

function addNotification(
user,
message
){

let notifications =
JSON.parse(
localStorage.getItem(
"notifications"
)
) || [];

notifications.push({

user:user,

message:message,

date:
new Date()
.toLocaleString()

});

localStorage.setItem(

"notifications",

JSON.stringify(
notifications
)

);

}

function showNotifications(){

showPage(
"notificationSection"
);

let notifications =
JSON.parse(
localStorage.getItem(
"notifications"
)
) || [];

let userNotifications =
notifications.filter(
n =>
n.user === currentUser
);

let html = "";

if(
userNotifications.length === 0
){

html =
`
<div class="notification">
No Notifications Available
</div>
`;

}else{

userNotifications.reverse();

userNotifications.forEach(n=>{

html +=

`
<div class="notification">

<b>🔔 Notification</b>

<br>

${n.message}

<br>

<small>
${n.date}
</small>

</div>
`;

});

}

document.getElementById(
"notificationData"
).innerHTML =
html;

}
function submitComplaint(){

let title =
document.getElementById(
"complaintTitle"
).value.trim();

let locationText =
document.getElementById(
"complaintLocation"
).value.trim();

let description =
document.getElementById(
"complaintDescription"
).value.trim();

let anonymous =
document.getElementById(
"anonymous"
).checked;

if(
title === "" ||
locationText === "" ||
description === ""
){

alert(
"Please fill all fields"
);

return;

}

let complaintId =
"CMP" +
Date.now();

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

complaints.push({

id:complaintId,

student:currentUser,

anonymous:anonymous,

title:title,

location:locationText,

description:description,

status:"Pending",

committee:"Not Assigned",

note:"",

date:
new Date()
.toLocaleDateString()

});

localStorage.setItem(

"complaints",

JSON.stringify(
complaints
)

);

addNotification(

currentUser,

"Complaint Submitted Successfully. ID: "
+
complaintId

);

alert(

"Complaint Submitted Successfully\n\nComplaint ID: "
+
complaintId

);

document.getElementById(
"complaintTitle"
).value="";

document.getElementById(
"complaintLocation"
).value="";

document.getElementById(
"complaintDescription"
).value="";

document.getElementById(
"anonymous"
).checked=false;

loadStudentStats();

}

function trackComplaint(){

let complaintId =
document.getElementById(
"trackId"
).value.trim();

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

let complaint =
complaints.find(
c =>
c.id === complaintId
);

if(!complaint){

document.getElementById(
"trackResult"
).innerHTML =

`
<div class="notification">
Complaint Not Found
</div>
`;

return;

}

document.getElementById(
"trackResult"
).innerHTML =

`

<div class="card">

<h3>
${complaint.id}
</h3>

<p>
<b>Title:</b>
${complaint.title}
</p>

<p>
<b>Status:</b>
${complaint.status}
</p>

<p>
<b>Committee:</b>
${complaint.committee}
</p>

<p>
<b>Committee Notes:</b>
${complaint.note || "No Notes"}
</p>

<p>
<b>Date:</b>
${complaint.date}
</p>

</div>

`;

}

function showHistory(){

showPage(
"historySection"
);

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

let filtered =
currentRole === "admin"
?
complaints
:
complaints.filter(
c =>
c.student === currentUser
);

let html =

`
<table>

<tr>

<th>ID</th>

<th>Title</th>

<th>Status</th>

<th>Committee</th>

<th>Date</th>

</tr>
`;

if(filtered.length===0){

html +=

`
<tr>

<td colspan="5">
No Complaints Found
</td>

</tr>
`;

}else{

filtered.forEach(c=>{

html +=

`

<tr>

<td>${c.id}</td>

<td>${c.title}</td>

<td>${c.status}</td>

<td>${c.committee}</td>

<td>${c.date}</td>

</tr>

`;

});

}

html += "</table>";

document.getElementById(
"historyData"
).innerHTML =
html;

}

function showCommitteeNotes(){

showPage(
"committeeNotesSection"
);

let note =
localStorage.getItem(
"generalCommitteeNote"
) || "";

document.getElementById(
"generalCommitteeNote"
).value =
note;

document.getElementById(
"savedNoteArea"
).innerHTML =

note
?

`
<div class="notification">

<b>Saved Committee Note:</b>

<br><br>

${note}

</div>
`

:

"";

}

function saveGeneralNote(){

let note =
document.getElementById(
"generalCommitteeNote"
).value;

localStorage.setItem(
"generalCommitteeNote",
note
);

document.getElementById(
"savedNoteArea"
).innerHTML =

`
<div class="notification">

<b>Saved Committee Note:</b>

<br><br>

${note}

</div>
`;

alert(
"Committee Note Saved"
);

}
function loadStudentStats(){

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

let myComplaints =
complaints.filter(
c =>
c.student === currentUser
);

document.getElementById(
"studentComplaints"
).innerText =
myComplaints.length;

document.getElementById(
"studentPending"
).innerText =
myComplaints.filter(
c =>
c.status === "Pending" ||
c.status === "Under Review"
).length;

document.getElementById(
"studentResolved"
).innerText =
myComplaints.filter(
c =>
c.status === "Resolved"
).length;

}

function loadAdminStats(){

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

document.getElementById(
"totalComplaints"
).innerText =
complaints.length;

document.getElementById(
"pendingComplaints"
).innerText =
complaints.filter(
c =>
c.status === "Pending" ||
c.status === "Under Review"
).length;

document.getElementById(
"resolvedComplaints"
).innerText =
complaints.filter(
c =>
c.status === "Resolved"
).length;

}

function loadComplaintsAdmin(){

showPage(
"adminComplaints"
);

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

let html =

`
<table>

<tr>

<th>ID</th>

<th>Student</th>

<th>Anonymous</th>

<th>Title</th>

<th>Status</th>

<th>Committee</th>

<th>Notes</th>

<th>Actions</th>

</tr>
`;

if(complaints.length === 0){

html +=

`
<tr>

<td colspan="8">
No Complaints Submitted
</td>

</tr>
`;

}else{

complaints.forEach(
(c,index)=>{

html +=

`

<tr>

<td>${c.id}</td>

<td>
${c.anonymous
?
"Anonymous"
:
c.student}
</td>

<td>
${c.anonymous
?
"Yes"
:
"No"}
</td>

<td>${c.title}</td>

<td>${c.status}</td>

<td>

<select
onchange=
"assignCommittee(
${index},
this.value
)">

<option>
${c.committee}
</option>

<option>
Anti-Ragging Committee
</option>

<option>
Disciplinary Committee
</option>

<option>
Student Welfare Committee
</option>

</select>

</td>

<td>

<input

value="${c.note}"

placeholder="Committee Notes"

onchange=
"saveNote(
${index},
this.value
)">

</td>

<td>

<button
class="action-btn warning"
onclick=
"changeStatus(
${index},
'Pending'
)">
Pending
</button>

<button
class="action-btn primary"
onclick=
"changeStatus(
${index},
'Under Review'
)">
Review
</button>

<button
class="action-btn success"
onclick=
"changeStatus(
${index},
'Resolved'
)">
Resolve
</button>

</td>

</tr>

`;

});

}

html += "</table>";

document.getElementById(
"adminComplaintData"
).innerHTML =
html;

}

function assignCommittee(
index,
committee
){

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

complaints[index].committee =
committee;

localStorage.setItem(

"complaints",

JSON.stringify(
complaints
)

);

addNotification(

complaints[index].student,

"Committee Assigned: "
+
committee
+
" for Complaint "
+
complaints[index].id

);

loadComplaintsAdmin();

}

function saveNote(
index,
note
){

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

complaints[index].note =
note;

localStorage.setItem(

"complaints",

JSON.stringify(
complaints
)

);

}
function changeStatus(
index,
status
){

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

complaints[index].status =
status;

localStorage.setItem(

"complaints",

JSON.stringify(
complaints
)

);

addNotification(

complaints[index].student,

"Complaint "
+
complaints[index].id
+
" status changed to "
+
status

);

loadComplaintsAdmin();

loadAdminStats();

}

function exportCSV(){

let complaints =
JSON.parse(
localStorage.getItem(
"complaints"
)
) || [];

let rows = [];

rows.push([

"Complaint ID",

"Student",

"Anonymous",

"Title",

"Location",

"Description",

"Status",

"Committee",

"Committee Notes",

"Date"

]);

complaints.forEach(c=>{

rows.push([

c.id,

c.student,

c.anonymous
?
"Yes"
:
"No",

c.title,

c.location,

c.description,

c.status,

c.committee,

c.note,

c.date

]);

});

let csvContent =

rows.map(row =>

row.map(cell =>

`"${String(cell || "").replace(/"/g,'""')}"`
).join(",")

).join("\n");

let blob =
new Blob(
[csvContent],
{
type:
"text/csv;charset=utf-8;"
}
);

let link =
document.createElement("a");

link.href =
URL.createObjectURL(blob);

link.download =
"AntiRaggingReport.csv";

document.body.appendChild(
link
);

link.click();

document.body.removeChild(
link
);

}

window.onload = function(){

let users =
JSON.parse(
localStorage.getItem(
"users"
)
) || [];

let adminExists =
users.find(
u =>
u.username === "admin"
);

if(!adminExists){

users.push({

username:"admin",

password:"admin123",

role:"admin"

});

localStorage.setItem(

"users",

JSON.stringify(
users
)

);

}

loadAdminStats();

};
</script>

</body>

</html>